import React from 'react'

import {
  FormattedIconContainer
} from './custom-button.styles'

const FormattedIcon = ({children, ...otherProps}) => {
    return (
      <FormattedIconContainer>

      </FormattedIconContainer>
    )
}

export default FormattedIcon